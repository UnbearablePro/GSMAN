class OnOpenHandler {

    static handleOpen() {
        // if (!Owner.exist()) {
        //     Installer.askForInstall();
        // } else {
            MenuOVB.createOVBMenu();
        // }
    }
}