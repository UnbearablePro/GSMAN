class OnOpenHandler {

    static handleEvent() {
        // if (!Owner.exist()) {
        //     Installer.askForInstall();
        // } else {
            MenuOVB.createOVBMenu();
        // }
    }
}