function openMeeting() {
    MeetingHandler.openMeeting();
}

function setMeetingFromStatusEvent(row, currentValue, oldValue, numePrenume, day, month, year, hour, minute, description) {
    MeetingHandler.setMeetingFromStatusEvent(row, currentValue, oldValue, numePrenume, day, month, year, hour, minute, description);
}