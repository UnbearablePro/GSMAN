function openMeeting() {
    ContacteSheetController.openMeeting();
}

function programMeetingFromStatusEvent(event, date, hour, details, reminder) {
    ContacteSheetController.programMeetingFromStatusEvent(event, date, hour, details, reminder);
}
function programMeeting(index, type, date, hour, details, reminder) {
    ContacteSheetController.programMeeting(index, type, date, hour, details, reminder);
}