function openMeeting() {
    MeetingHandler.openMeeting();
}

function programMeetingFromStatusEvent(event, date, hour, details, reminder) {
    MeetingHandler.programMeetingFromStatusEvent(event, date, hour, details, reminder);
}
function programMeeting(index, type, date, hour, details, reminder) {
    MeetingHandler.programMeeting(index, type, date, hour, details, reminder);
}